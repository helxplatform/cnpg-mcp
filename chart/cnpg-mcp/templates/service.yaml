apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnpg-mcp.fullname" . }}
  labels:
    {{- include "cnpg-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cnpg-mcp.selectorLabels" . | nindent 4 }}
