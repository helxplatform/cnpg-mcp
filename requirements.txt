# Core MCP Server dependencies
mcp>=1.0.0

# Kubernetes client
kubernetes>=28.0.0

# Validation and type checking
pydantic>=2.0.0

# YAML processing (for dry_run cluster definitions)
pyyaml>=6.0.0

# Async support (usually included with Python 3.7+, but explicit for clarity)
asyncio

# ============================================================================
# Optional: HTTP/SSE Transport (uncomment when implementing HTTP mode)
# ============================================================================
# Uncomment these when you're ready to implement HTTP transport:
# mcp[sse]>=1.0.0
# starlette>=0.35.0
# uvicorn>=0.27.0
# python-multipart>=0.0.6  # For file uploads if needed
